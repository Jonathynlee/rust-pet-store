# Run the app
To run the app simply:
- open the app directory
- run `cargo run`

# Additionaly information
The app isn't perfect, but simply relfects what I have learned so far. Feel free to extend the app to make it more interesting!
